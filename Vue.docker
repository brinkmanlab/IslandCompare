FROM node:alpine

USER node

RUN mkdir -p /home/node/app

WORKDIR /home/node/app

# copy both 'package.json' and 'package-lock.json' (if available)
COPY --chown=node package*.json ./

USER root

RUN npm install -g --dev

EXPOSE 8080

CMD npm link && npm run serve
